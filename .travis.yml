sudo: false

language: ruby

rvm:
  - 2.4.0

cache: bundler
script: bundle exec middleman build

deploy:
  provider: script
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  script: bash ./deploy.sh
  on:
    branch: master
