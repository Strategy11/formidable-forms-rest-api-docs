sudo: false

language: ruby

rvm:
  - 2.4.0

cache: bundler
script: bundle exec middleman build

deploy:
  provider: script
  skip_cleanup: true
  script: "./deploy.sh"
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  api_key:
    secure: $GITHUB_TOKEN
  on:
    branch: master
