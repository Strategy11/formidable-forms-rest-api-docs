sudo: false

language: ruby

rvm:
  - 2.4.0

cache: bundler
script: bundle exec middleman build

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: source
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
